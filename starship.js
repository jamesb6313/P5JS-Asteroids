class Starship extends Matter.Body {

	constructor(x, y, r, a) {
		super(x, y, r, a);
		
		/* this.r = r;
		
		this.deltaHealth = 0.05;
		this.health = 1.0;

		const options = {
			friction: 0.0,
			frictionAir : 0.01,
			restitution: 0.3,
			angle: a,
			label: "ship"
		}
		this.body = Bodies.circle(x, y, r, options);
		Body.setMass(this.body, this.body.mass*4);
		World.add(world, this.body); */
		
	}
	
	
}